import { WarniningDirective } from './warnining.directive';

describe('WarniningDirective', () => {
  it('should create an instance', () => {
    const directive = new WarniningDirective();
    expect(directive).toBeTruthy();
  });
});
